apiVersion: v1
kind: Namespace
metadata:
  name: ${HELLOORLEONS_NAMESPACE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${HELLOORLEONS_SA_NAME}
  namespace: ${HELLOORLEONS_NAMESPACE}
  annotations:
    azure.workload.identity/client-id: ${HELLOORLEONS_IDENTITY_CLIENT_ID}
  labels:
    azure.workload.identity/use: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloorleons
  namespace: ${HELLOORLEONS_NAMESPACE}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: helloorleons
  template:
    metadata:
      labels:
        app: helloorleons
        azure.workload.identity/use: "true"
        orleans/serviceId: helloorleons
        orleans/clusterId: helloorleons-${STAMP_NAME}
    spec:
      terminationGracePeriodSeconds: 65
      serviceAccountName: ${HELLOORLEONS_SA_NAME}
      containers:
        - name: helloorleons
          image: ${ACR_LOGIN_SERVER}/helloorleons:1775932925-571f6f18
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 11111
              name: silo
            - containerPort: 30000
              name: gateway
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "5"]
          env:
            - name: AZURE_CLIENT_ID
              value: ${HELLOORLEONS_IDENTITY_CLIENT_ID}
            - name: APPLICATIONINSIGHTS_CONNECTION_STRING
              value: ${APPLICATIONINSIGHTS_CONNECTION_STRING}
            - name: OTEL_SERVICE_NAME
              value: helloorleons
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: deployment.environment=${STAMP_NAME},cloud.region=${REGION},stamp=${STAMP_KEY}
            - name: OTEL_TRACES_SAMPLER
              value: "parentbased_traceidratio"
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "0.01"
            - name: AI__Endpoint
              value: ${AI_SERVICES_ENDPOINT}
            - name: AI__ModelName
              value: ${AI_MODEL_GPT41}
            - name: CosmosDb__DatabaseName
              value: ${HELLOORLEONS_COSMOS_DATABASE}
            - name: CosmosDb__ContainerName
              value: ${HELLOORLEONS_COSMOS_CONTAINER}
            - name: CosmosDb__ClusterContainerName
              value: ${HELLOORLEONS_COSMOS_CLUSTER_CONTAINER}
            - name: ConnectionStrings__cosmos
              value: "AccountEndpoint=${COSMOS_ENDPOINT}"
          resizePolicy:
            - resourceName: cpu
              restartPolicy: NotRequired
            - resourceName: memory
              restartPolicy: NotRequired
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: helloorleons
  namespace: ${HELLOORLEONS_NAMESPACE}
spec:
  selector:
    app: helloorleons
  ports:
    - port: 8080
      targetPort: 8080
