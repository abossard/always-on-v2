apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: helloagents
  namespace: flux-system
spec:
  image: ${ACR_LOGIN_SERVER}/helloagents
  interval: 1m
  provider: azure
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: helloagents
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: helloagents
  filterTags:
    pattern: '^(?P<ts>[0-9]+)-[a-f0-9]{8}$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
