apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: graphorleons-http-redirect
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  parentRefs:
    - name: events-gateway
      namespace: aks-istio-ingress
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: graphorleons-api-route
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  parentRefs:
    - name: events-gateway
      namespace: aks-istio-ingress
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        - name: graphorleons
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /health
      backendRefs:
        - name: graphorleons
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /dashboard
      backendRefs:
        - name: graphorleons
          port: 8080
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: graphorleons-web
          port: 8080
