apiVersion: v1
kind: Namespace
metadata:
  name: ${GRAPHORLEONS_NAMESPACE}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${GRAPHORLEONS_SA_NAME}
  namespace: ${GRAPHORLEONS_NAMESPACE}
  annotations:
    azure.workload.identity/client-id: ${GRAPHORLEONS_IDENTITY_CLIENT_ID}
  labels:
    azure.workload.identity/use: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphorleons
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graphorleons
  template:
    metadata:
      labels:
        app: graphorleons
        azure.workload.identity/use: "true"
        orleans/serviceId: graphorleons
        orleans/clusterId: graphorleons-${STAMP_NAME}
    spec:
      terminationGracePeriodSeconds: 65
      serviceAccountName: ${GRAPHORLEONS_SA_NAME}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: graphorleons
      containers:
        - name: graphorleons
          image: ${ACR_LOGIN_SERVER}/graphorleons:1777125659-fb0baa73
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 11111
              name: silo
            - containerPort: 30000
              name: gateway
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "5"]
          env:
            - name: AZURE_CLIENT_ID
              value: ${GRAPHORLEONS_IDENTITY_CLIENT_ID}
            - name: APPLICATIONINSIGHTS_CONNECTION_STRING
              value: ${APPLICATIONINSIGHTS_CONNECTION_STRING}
            - name: OTEL_SERVICE_NAME
              value: graphorleons
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: deployment.environment=${STAMP_NAME},cloud.region=${REGION},stamp=${STAMP_KEY}
            - name: OTEL_TRACES_SAMPLER
              value: "parentbased_traceidratio"
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "0.01"
            - name: ConnectionStrings__orleans-cosmos
              value: "AccountEndpoint=${ORLEANS_COSMOS_ENDPOINT}"
            - name: ConnectionStrings__cosmos
              value: "AccountEndpoint=${COSMOS_ENDPOINT}"
            - name: AlwaysOn__GrainStorage__Endpoint
              value: "AccountEndpoint=${COSMOS_ENDPOINT}"
            - name: ORLEANS_CLUSTER_ID
              value: graphorleons-${STAMP_NAME}
            - name: ORLEANS_SERVICE_ID
              value: graphorleons
            - name: AlwaysOn__GrainStorage__Database
              value: ${GRAPHORLEONS_COSMOS_DATABASE}
            - name: AlwaysOn__GrainStorage__Container
              value: graphorleons-grainstate
            - name: AlwaysOn__GrainStorage__Name
              value: GrainState
            - name: AlwaysOn__Clustering__Endpoint
              value: "AccountEndpoint=${ORLEANS_COSMOS_ENDPOINT}"
            - name: AlwaysOn__Clustering__Container
              value: graphorleons-cluster
            - name: AlwaysOn__Clustering__Database
              value: ${ORLEANS_CLUSTER_DB}
            - name: AlwaysOn__PubSub__Container
              value: graphorleons-pubsub
            - name: CosmosDb__ModelsContainerName
              value: graphorleons-models
            - name: ConnectionStrings__graphorleons-events
              value: ${GRAPHORLEONS_EVENTHUB_ENDPOINT}
            - name: ConnectionStrings__queues
              value: ${GRAPHORLEONS_STORAGE_QUEUE_ENDPOINT}
          resizePolicy:
            - resourceName: cpu
              restartPolicy: NotRequired
            - resourceName: memory
              restartPolicy: NotRequired
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: graphorleons
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  selector:
    app: graphorleons
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: graphorleons
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: graphorleons
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphorleons-web
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graphorleons-web
  template:
    metadata:
      labels:
        app: graphorleons-web
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: graphorleons-web
      containers:
        - name: graphorleons-web
          image: ${ACR_LOGIN_SERVER}/graphorleons-web:1777125659-fb0baa73
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: graphorleons-web
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  selector:
    app: graphorleons-web
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: graphorleons-web
  namespace: ${GRAPHORLEONS_NAMESPACE}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: graphorleons-web
