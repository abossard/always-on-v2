version: v0.1
testId: graphorleons-load-test
displayName: GraphOrleons Load Test
description: Multi-tenant mixed load — 50 tenants, instrument events, graph edges, SSE streams
testType: Locust
testPlan: locustfile.py
engineInstances: 1
failureCriteria:
  - avg(response_time_ms) > 500
  - percentage(error) > 5
autoStop:
  errorPercentage: 90
  timeWindow: 30
